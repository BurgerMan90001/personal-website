export const loggingEnabled: boolean = true;
export const port: string = process.env.PORT || "3000"; // Take a port 3000 for running server.
export const siteUrl: string = `http://localhost:${port}/`;
