"use strict";
//import { http } from './request'
Object.defineProperty(exports, "__esModule", { value: true });
/*
export default {
  getById(id: string) {
    if (id) { // not empty

      return http.get(`/tasks/products/${id}`)
    }
    else {
      return http.get('/tasks/products' + location.search)
    }
  },
  /*
  create(data) {

    if (data) {
      return http.post('/tasks/products', data)
    }
    else {
      return http.get('/tasks/products/create')
    }
  },
  edit(id: string, data: any) {
    if (data) {
      return http.put(`/tasks/products/${id}`, data)
    }
    else {
      return http.get(`/tasks/products/${id}`)
    }
  },
  delete(id: string, data) {
    if (data) {
      return http.delete(`/tasks/products/${id}`)
    }
    else {
      return http.get(`/tasks/products/${id}`)
    }
  }
*/
