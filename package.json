{
  "name": "websitev2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "start": "",
    "start-server": "",
    "start-client": "node dist/src/server.js",
    "build": "rimraf dist && cd client && npm run build && cd.. && cd server && npm run build && cd..",

    
    "lint": "eslint . --fix",
    "format": "prettier --write src/",
    "test:unit": "vitest",
    "test:e2e": "playwright test"
  }
}
